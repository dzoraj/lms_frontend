import { NoDataDirective } from './no-data.directive';

describe('NoDataDirective', () => {
  it('should create an instance', () => {
    const directive = new NoDataDirective();
    expect(directive).toBeTruthy();
  });
});
